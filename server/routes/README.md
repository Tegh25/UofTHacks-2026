# API Routes

- `intake.js` — `POST /intake`: creates the patient record, responds
  immediately, and runs the agent pipeline in the background
- `patients.js` — `GET /patients`, `GET /patients/:id`
- `demo.js` — `POST /clear-demo-data`
